# 生产环境 vs 开发环境 .env

一共涉及三个 env 文件，搞清楚各自用途：

| 文件 | 用途 | 值 |
|------|------|-----|
| `env.dev` | 开发环境，接本地服务 | 真实值直接写 |
| `env.prod` | **占位符**，映射变量名 | `${变量}` 占位 |
| `env.docker`（被 yml 引用） | yml 外面接的 env，透过 yml 传给 Docker 容器 | 真实密码、Key |
